package dev.fespinosa.algorithms.arrays;

/**
 * Array
 */
public class Array {

	public static int[] of(int i) {
		int[] numbers = new int[i]
		for (int i = 0; i < n; i++) {
            int aItem = Integer.parseInt(numbers[i]);
            a[i] = aItem;
        }
		return numbers;
	}

    
}